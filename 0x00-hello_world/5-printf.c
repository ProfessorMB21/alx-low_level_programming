#include <stdio.h>

/**
 * main - A program to print to the console using printf
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char f[] = "with proper grammar";
	char l[] = "but the outcome is a piece of art";

	printf("%s, %s,\n", f, l);
	return (0);
}
