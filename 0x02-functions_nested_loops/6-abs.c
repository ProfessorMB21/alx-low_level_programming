/* Preprocessors */
#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @int: int
 *
 * Return: int (sucess)
 */

int _abs(int)
{
	if (int < 0)
	{
		return (int * -1);
	} else if (int > 0)
	{
		return (int);
	}
	return (0);
}
